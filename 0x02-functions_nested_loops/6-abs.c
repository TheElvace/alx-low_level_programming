#include "main.h"

/**
 * _abs - This computes the absolute value of an integer.
 * @n: This is the integer that is to be computed.
 *
 * Return: This is the absoluter vakue of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
